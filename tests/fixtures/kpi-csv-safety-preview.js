// Harmless formula-like TEXT for local export QA only. Nothing is evaluated.
// This route reuses the real Blob capture and all application styles/dispatchers.
window.kpiObjectives[0].name='=1+2';
Object.assign(window.kpiKPIs[0],{code:'-1.1',name:'+Toolbox sample',responsible:'@Sample reviewer'});
Object.assign(window.kpiIndicators[0],{name:'＝Sample indicator',unit:'count',target_operator:'eq'});
